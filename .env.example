# Used by docker-compose.yml (docker compose reads a .env file in this
# directory automatically). Copy to .env and fill in as needed - both are
# optional, see docker-compose.yml's comments.
GOOGLE_GEOCODING_API_KEY=
ADVISORY_LLM_ENABLED=true
